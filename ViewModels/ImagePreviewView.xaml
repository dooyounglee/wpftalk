<Window x:Class="talk2.ViewModels.ImagePreviewView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:talk2.ViewModels"
        mc:Ignorable="d"
        Title="ImagePreviewView" Height="450" Width="300">
        <ScrollViewer x:Name="scrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <Grid>
                <Image x:Name="imageControl" Source="{Binding Image}" MouseWheel="Image_MouseWheel" HorizontalAlignment="Left" VerticalAlignment="Top" Stretch="UniformToFill">
                    <!--<Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="imageScale" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="imageTranslate" X="0" Y="0"/>
                        </TransformGroup>
                    </Image.RenderTransform>-->
                    <Image.LayoutTransform>
                        <ScaleTransform x:Name="imageScale" ScaleX="1" ScaleY="1" />
                    </Image.LayoutTransform>
            </Image>
            </Grid>
        </ScrollViewer>
    <!--<ScrollViewer ScrollViewer.CanContentScroll="False">
        <Grid>
            <Image Source="{Binding Image}" HorizontalAlignment="Left" VerticalAlignment="Top" Stretch="Uniform">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleX="{Binding ScaleX}" ScaleY="{Binding ScaleY}"/>
                        <TranslateTransform X="0" Y="0"/>
                    </TransformGroup>
                </Image.RenderTransform>
                <Image.InputBindings>
                    <MouseBinding MouseAction="LeftClick"
                        Command="{Binding ZoomInCommand}"
                        />
                    <MouseBinding MouseAction="RightClick"
                        Command="{Binding ZoomOutCommand}"
                        />
                </Image.InputBindings>
            </Image>
        </Grid>
    </ScrollViewer>-->
</Window>
