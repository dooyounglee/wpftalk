<UserControl x:Class="talk2.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:talk2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300"
             x:Name="root">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Templates/UsersTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" TextWrapping="Wrap" Text="{Binding Me.UsrNm}">
                <TextBlock.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding UserInfoCommand}" CommandParameter="{Binding Me.UsrNo}"/>
                </TextBlock.InputBindings>
            </TextBlock>
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding MyConnStateItems}"
                      SelectedValue="{Binding SelectedMyConnState, Mode=TwoWay}"
                      SelectedValuePath="State"
                      DisplayMemberPath="Display" />
            <Button Grid.Column="2" Content="Logout" Command="{Binding LogoutCommand}" />
            <Button Grid.Column="3" Content="New" Command="{Binding CreateUserCommand}" />
            <Button Grid.Column="4" Content="Div" Command="{Binding DivCommand}" />
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchUsrNm, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding SearchConnStateItems}"
                      SelectedValue="{Binding SearchConnState}"
                      SelectedValuePath="State"
                      DisplayMemberPath="Display"/>
            <ComboBox Grid.Column="2" ItemsSource="{Binding SearchDivItems}"
                      SelectedValue="{Binding SearchDiv}"
                      SelectedValuePath="DivNo"
                      DisplayMemberPath="DivNm"/>
            <Button Grid.Column="3" Content="새로고침" Command="{Binding ReloadUserCommand}" />
        </Grid>
        <Grid Grid.Row="3">
            <ScrollViewer x:Name="scroll">
                <ItemsControl ItemsSource="{Binding UserList}" ItemTemplate="{StaticResource UsersTemplate}" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
