<Window x:Class="talk2.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:local="clr-namespace:talk2.Views"
        xmlns:viewmodels="clr-namespace:talk2.ViewModels"
        mc:Ignorable="d" 
        Title="MainView" Height="450" Width="300" Closing="Window_Closing">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
            <local:LoginView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:UserViewModel}">
            <local:UserView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ChatViewModel}">
            <local:ChatView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:RoomViewModel}">
            <local:RoomView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SettingViewModel}">
            <local:SettingView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="{Binding MenuHeight}" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding CurrentViewModel}" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="Transparent">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding GotoUserCommand}"/>
                </Border.InputBindings>
                <Grid Background="{Binding ColorUser}">
                    <TextBlock Text="👤" FontSize="35" HorizontalAlignment="Center" VerticalAlignment="Center" AutomationProperties.AutomationId="menuUser"/>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="Transparent">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding GotoChatCommand}"/>
                </Border.InputBindings>
                <Grid Background="{Binding ColorChat}">
                    <TextBlock Text="💭" FontSize="35" HorizontalAlignment="Center" VerticalAlignment="Center" AutomationProperties.AutomationId="menuChat"/>
                </Grid>
            </Border>
            <Border Grid.Column="2" Background="Transparent">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding GotoSettingCommand}"/>
                </Border.InputBindings>
                <Grid Background="{Binding ColorSetting}">
                    <TextBlock Text="⚙️" FontSize="35" HorizontalAlignment="Center" VerticalAlignment="Center" AutomationProperties.AutomationId="menuSetting"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
